package com.example.photogallery;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {
        assertEquals(4, 2 + 2);
    }

    /*
        All acceptance tests are written to fail, but thereafter the functionality is incrementally added so that all tests pass.
        To ensure that ATDD delivers the right product, customer, developers, and testers should collaborate to formalize the correct acceptance tests.
     */
}

